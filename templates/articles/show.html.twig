{% extends 'base.html.twig' %}
{% block title %}{{ article.nom }}{% endblock %}
{% block body %}
    <h1>{{ article.nom }}</h1>
    <p>{{ article.description }}</p>
    <p>{{ article.image }}</p>
    <p>{{ article.prix }}</p>
    <p>{{ article.categories.titre }}</p>
    <hr>
<a href="/">Retour</a>
{% endblock %}